name: created issue
on:
 issues:
   type: [opend]
job:
  issue:
    runs-on: ubuntu-latest
    steps:
      - name: "dumo github context"
        runs: echo '${{ toJSON(github.event) }}' | jq
        shell: bash
